import { StyleSheet, Text, View } from 'react-native';
import React from 'react';
import Header from '../components/Header';

export default function calls() {
  return <Header title="Calls" />;
}

const styles = StyleSheet.create({});
